.venv/
__pycache__/
*.pyc
.env
.DS_Store

# Python packages (should never be committed)
Lib/
etc/
share/

# Local data and artifacts
# data.sqlite  # Temporarily commented for deployment
# artifacts/  # Commented out to allow GitHub Actions to commit trained models
# artifacts_optimized/  # Commented out to allow GitHub Actions to commit optimized models

# Streamlit local config
.streamlit/secrets.toml

# Large model/files (excluding trained models for deployment)
# *.pkl  # Commented out - trained models needed for deployment
*.csv

# Firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
.firebaserc.local

# Google Cloud credentials (NEVER commit these!)
*.json
!package.json
!package-lock.json
!tsconfig.json
!firebase.json
!firestore.indexes.json
!firestore.rules
!storage.rules
gha-creds-*.json
*-creds-*.json
*credentials*.json
*service-account*.json

# Node modules
node_modules/
public/node_modules/
functions/node_modules/

# React build
public/build/
public/.env.local
public/.env.development.local
public/.env.test.local
public/.env.production.local

# Python virtual environment
venv/
ENV/
env/
