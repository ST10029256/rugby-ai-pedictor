.venv/
__pycache__/
*.pyc
.env
.env.local
rugby-ai-predictor/.env.local
.DS_Store

# Python packages (should never be committed)
Lib/
etc/
share/

# Local data and artifacts
# data.sqlite  # Temporarily commented for deployment
# artifacts/  # Commented out to allow GitHub Actions to commit trained models
# artifacts_optimized/  # Commented out to allow GitHub Actions to commit optimized models

# Streamlit local config
.streamlit/secrets.toml

# Large model/files (excluding trained models for deployment)
# *.pkl  # Commented out - trained models needed for deployment
*.csv

# Firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
.firebaserc.local

# Google Cloud credentials (NEVER commit these!)
*.json
!package.json
!package-lock.json
!tsconfig.json
!firebase.json
!firestore.indexes.json
!firestore.rules
!storage.rules
!last_checkpoint.json
!artifacts/maz_maxed_v4_metrics_latest.json
!artifacts/maz_maxed_v4_prod_latest.json
!artifacts/maz_maxed_v5_metrics_latest.json
!artifacts/maz_maxed_v5_prod_latest.json
!artifacts/league_model_champions.json
!rugby-ai-predictor/league_model_champions.json
gha-creds-*.json
*-creds-*.json
*credentials*.json
*service-account*.json

# Node modules
node_modules/
public/node_modules/
functions/node_modules/

# React build
public/build/
public/.env.local
public/.env.development.local
public/.env.test.local
public/.env.production.local

# Python virtual environment
venv/
ENV/
env/

# Large video files (exceed GitHub's 100MB limit)
*.mov
*.mp4
public/public/*.mov
public/public/*.mp4
public/*.mov
# Timestamped SQLite backups written by scripts/fix_team_identity.py
data.backup_*.sqlite

# Data files generated by scripts/ but imported directly by the React build.
# The blanket *.json rule above would drop them and break `npm run build`.
!public/src/data/leagueSeasonWindows.json
!public/src/utils/leagueTeamsFallback.json
!public/src/utils/staticTeamLogos.json

# Experiment output from scripts/killer*. Reproducible from that code, and the
# checkpoints alone are ~36 MB.
artifacts_killer*/

# Downloaded tooling and packaged copies of landing-dist/
tools/
website-landing.zip

# Scratch probes and audit logs
rugby-ai-predictor/_tmp_*.py
rugby-ai-predictor/audit_jerseys_run.log
