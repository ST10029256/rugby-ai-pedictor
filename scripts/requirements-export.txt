# Minimal deps for scripts/export_pre_kickoff_replay_excel.py
# Do NOT install full requirements.txt on Python 3.13 (sklearn 1.3.2 won't build).
# Model .pkl meta files embed sklearn calibrators — sklearn is required to load them.

pandas>=2.0.0,<2.3.0
openpyxl>=3.1.0
numpy>=1.24.0,<2.0.0
scikit-learn>=1.5.0
joblib>=1.3.0
torch
requests>=2.32.0
